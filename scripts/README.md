# lipsum-cpp scripts

There are two files in this folder: 

- [`split.py`](./split.py)
- [`lipsum.txt`](./lipsum.txt)

`split.py` is used to count the words in the sample `lipsum.txt` and form it into an `std::vector<std::string>`, stored in [`src/lipsum/sample.inl`](../src/lipsum/sample.inl).
