# cmake/

This directory contains snippets of CMake code to split the main [`CMakeLists.txt`](../CMakeLists.txt) into multiple files. It also contains a toolchain file for testing Windows development from Linux.
