# lipsum-cpp JS binding

This is a binding to JavaScript using embind + Emscripten. To use, call functions and create objects with the `lpsm_` prefix before `Module`. Just like the C wrapper, manually provide arguments like with the C wrapper. See [`JSBinding.html`](../../examples/JSBinding.html) for more info.
